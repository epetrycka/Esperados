Saluton

difini zapisz(snuro: napis) {
    se(napis ne egala "ostatni"){
        tutmonda variablo result asigini napis
    }
    reveni
    tutmonda variablo result asigini napis
}

funcio zapisz(napis = "jeden")
funcio zapisz(napis = "drugi")
funcio zapisz(napis = "trzeci")
funcio zapisz(napis = "czwarty")
funcio zapisz(napis = "ostatni")

Adiau